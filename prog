!pip install pandas matplotlib scikit-learn

import pandas as pd
import matplotlib.pyplot as plt

from sklearn.model_selection import train_test_split
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import accuracy_score, confusion_matrix

# Load dataset
df = pd.read_csv("brain_cancer_data.csv")

print("Dataset Loaded Successfully\n")
print(df)

print("\nDataset Information:")
print(df.info())

print("\nMissing Values:")
print(df.isnull().sum())

# Features
X = df[["Age", "Headache", "MRI_Score", "Tumor_Size"]]

# Target
y = df["Cancer"]

print("\nInput Features:")
print(X.head())

print("\nTarget:")
print(y.head())
X_train, X_test, y_train, y_test = train_test_split(
    X,
    y,
    test_size=0.20,
    random_state=42
)
model = LogisticRegression(max_iter=1000)

model.fit(X_train, y_train)

print("Model Trained Successfully")
predictions = model.predict(X_test)

print("Predictions:")
print(predictions)
accuracy = accuracy_score(y_test, predictions)

print(f"\nModel Accuracy: {accuracy*100:.2f}%")

cm = confusion_matrix(y_test, predictions)

print("\nConfusion Matrix:")
print(cm)

new_patient = pd.DataFrame(
    [[42, 5, 80, 4]],
    columns=["Age", "Headache", "MRI_Score", "Tumor_Size"]
)

result = model.predict(new_patient)

print("\nPrediction Result:")

if result[0] == 1:
    print("⚠ Brain Cancer Detected")
else:
plt.figure(figsize=(6,4))

df["Cancer"].value_counts().plot(kind="bar")

plt.title("Cancer vs Normal Patients")
plt.xlabel("Condition")
plt.ylabel("Count")

plt.xticks([0,1], ["Cancer", "Normal"], rotation=0)

plt.show()

plt.figure(figsize=(5,5))

df["Cancer"].value_counts().plot(
    kind="pie",
    autopct="%1.1f%%"
)

plt.title("Cancer Distribution")
plt.ylabel("")

plt.show()


    print("✅ Normal Condition")print("\nActual Values:")
print(y_test.values)
